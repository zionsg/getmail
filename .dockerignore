# Ignore these for security and also that Docker will take very long to read all
# the files inside the folders during build (cos they are in the context directory)
.git/
node_modules/
vendor/
.dockerignore
.gitignore
.npmrc
Dockerfile
npm-debug.log
